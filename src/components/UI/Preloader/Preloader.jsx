import React from 'react'
import preloader from '../../../assets/img/gifLoader.svg'

const Preloader = (props) => {
    return (
        <img src={preloader} alt={`preloader`}/>
    )
}

export default Preloader